{
    "additionalProperties": false,
    "error_msg": "The Tatoeba Challenge English-Spanish parameters must be one or more of ['epochs', 'batch size', 'weight decay'].",
    "description": "Tatoeba Challenge English-Spanish is a transformer that allows you to translate sentences from English to Spanish.",
    "properties": {
        "epochs": {
            "oneOf": [
                {
                    "error_msg": "An integer greater than or equal to 1 must be entered",
                    "description": "Number of epochs to fine-tune the model",
                    "type": "integer",
                    "minimum": 1,
                    "default": 1
                }
            ]
        },
        "batch_size": {
            "oneOf": [
                {
                  "error_msg": "An integer greater than or equal to 1 must be entered",
                  "description": "Size of the batches with which the training will be done",
                    "type": "integer",
                    "minimum": 1,
                    "default": 16
                }
            ]
        },
        "learning_rate": {
            "oneOf": [
                {
                    "error_msg": "A number between 10e-6 and 1 is recommended",
                    "description": "AdamW optimizer learning rate",
                    "type": "number",
                    "minimum": 0,
                    "default": 2e-5
                }
            ]
        },
        "device": {
            "oneOf": [
                {
                  "error_msg": "The parameter 'device' must be in string format and can be either 'gpu' or 'cpu'",
                  "description": "Hardware that will run the training. GPU is recommended due to efficiency issues",
                    "type": "string",
                    "default": "gpu",
                    "enum": ["gpu", "cpu"]
                }
            ]
        },
        "weight_decay": {
            "oneOf": [
                {
                  "error_msg": "A number between 0 and 1 must be entered",
                  "description": "Parameter indicating the level of penalty in the regularization",
                    "type": "number",
                    "minimum": 0,
                    "default": 0.01
                }
            ]
        }
    },
    "type": "object"
}
